import 'package:flutter/material.dart';

class SoundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sound Page'),
      ),
      body: Center(
        child: Text('This is the Sound Page'),
      ),
    );
  }
}