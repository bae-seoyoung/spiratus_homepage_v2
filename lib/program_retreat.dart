import 'package:flutter/material.dart';

class RetreatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Retreat Page'),
      ),
      body: Center(
        child: Text('This is the Retreat Page'),
      ),
    );
  }
}