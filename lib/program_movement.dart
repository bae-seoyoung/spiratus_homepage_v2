import 'package:flutter/material.dart';

class MovementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Movement Page'),
      ),
      body: Center(
        child: Text('This is the Movement Page'),
      ),
    );
  }
}